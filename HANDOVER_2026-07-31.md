# 인수인계 및 문서 최신화 목록 (2026-07-31)

> 이 문서는 2026-07-30~31 이틀간의 변경을 인수인계하고, 기존 md 문서에서
> 최신 코드와 어긋난 부분을 파일·섹션 단위로 지정한다.
> 정본 우선순위: 코드/봉인 산출물 > 이 문서 > 기존 md.

---

## 1. 현재 상태 스냅샷

| 항목 | 값 |
| --- | --- |
| baseline_commit (봉인) | `6ecd2c9` |
| prompt_bundle_sha256 | `9cb9c07a…` |
| 테스트 | 171건 전부 통과 |
| 오프라인 E2E | 45일 90/90 이벤트 완주, outcome_finalized |
| 유료 2일 검증 (v8) | **완주.** 400/400 턴, 소진 0, 최대 재시도 3/10, reasoning_tokens 0, $1.87 |
| v8 보존 위치 | `outputs/logs/live_2day_v8_20260731/` (git 추적, 99_validate `segment_valid_not_publication_ready` 통과) |
| 45일 본실험 | **준비 완료, 미시작** (사용자 승인 필요, 예상 $40~55, ON/OFF 2 arm) |
| 누적 유료 지출 | 약 $8~9 |

### 이틀간 커밋 요지 (오래된 것부터)

1. 커뮤니티 뱃지 3종 완전 제거 + 문서 반영 (`badge.py` 삭제)
2. `community_thinking` 인용을 **번호 기반**으로 재설계 (validator v4, 아래 §3.3)
3. STB dim_6 `정보 한계:`/`주의점:` 마커 강제, Step 2 재구성
4. LTB 재서술 규칙 완화: 차원별 강제 → **여섯 차원 전량 복사만 거부** (생성·저장 두 경계 동일화)
5. outcome 방향 규칙을 프롬프트에 명시 (양수=support, 음수=contradict)
6. 재시도 힌트에서 방향 유도 문구 제거 (연구자 편향 차단)
7. resume 시드 고정 유지 확정 (retry lineage 전면 롤백)
8. `llm_validation_errors.jsonl`을 롤백 면제 control artifact로 승격
9. `BELIEF_LIMITS` 전 차원 150자 통일 + config 단일 정본화
10. `34f2ff9`: LTB dim_6 검증 KeyError 수정(재시도 우회 차단) + `make_decision.txt`에 "필수 key만" 문구
11. `6ecd2c9`: **D2 검색 채점을 구 전체 일치 → 공백 토큰 일치로 수정** (§3.1)
12. 재봉인 2회 (`82902d9`, `2bffc2c`)

---

## 2. 반드시 알아야 할 운영 규칙 (코드에 이유 주석 있음)

- **게이트 순서: 전체 테스트 → 오프라인 E2E → 유료.** 순서를 건너뛴 실패 사례 2건 있음(BSD sed 사고, 저장 경계 미러 미수정).
- **오프라인 E2E는 `--max-agents 7` 이상으로 돌릴 것.** A001~A004에는 depth 2 에이전트가 없어(첫 D2는 A007) `--max-agents 4`로는 검색 코드가 실행조차 되지 않는다. D2 검색이 죽어 있던 것을 못 잡은 원인.
- **resume은 동일 시드·동일 프롬프트를 재생한다.** 결정론적 실패는 resume해도 같은 자리에서 재현된다(의도된 설계 — 시드 가변은 해석 가능성 훼손으로 사용자가 기각). 이런 실패는 원인 수정 → 커밋 → 재봉인 후 재실행해야 한다.
- **코드/프롬프트가 바뀌면 재봉인 + 진행 중 run의 journal은 무효** (`manifest_sha256` drift). 실행 중 핫픽스 불가.
- **유료 호출·본실험·커밋·푸시는 사용자 명시 승인 필요** (AGENTS.md 작업 원칙).
- 프롬프트에 구현 상세를 넣지 않는다. 근거 방향(support/contradict)에 연구자 편향을 주입하지 않는다.
- `outputs/logs/`는 git에 그대로 올린다(.gitignore 제외 금지 — 사용자 결정).

---

## 3. 문서별 최신화 필요 목록

### 3.1 `ARCHITECTURE.md` — **사실 불일치 (우선순위 높음)**

- **:436-440 벨리프 차원 표: `dim_2`~`dim_6` "100자" → 전부 150자.**
  실값: `config.BELIEF_LIMITS` = 전 차원 150, 봉인 spec 동일. 100자는 재시도
  33건 중 32건을 만들던 옛 한도다(config.py 주석 참조).
- **news_agent/D2 검색 절**: 검색 채점이 "키워드 공백 토큰 단위 부분 일치,
  제목 2배 가중, 0점 탈락, top 5"임을 명시. 2026-07-31 이전 코드는 구 전체
  일치라 라이브에서 항상 0건이었다는 사실을 이력으로 남길 것.
- :845 부근 Best score 서술은 현행과 일치 — 수정 불요.

### 3.2 `EXPERIMENT_DESIGN.md`

- **:202 dim_6 "100자" → 150자.** (:197 dim_1 150자는 맞음. 표 전체를 150으로 통일)
- **§8.4 community provenance 아래에 번호 인용 계약 추가** (§3.3 내용).
- dim_6 서술에 `정보 한계:` / `주의점:` 마커가 검증 강제라는 사실 추가 (:202 행).
- LTB 재서술 규칙: "여섯 차원 전량 verbatim 복사만 거부, 차원별 유지는 허용"
  명시 (임베딩 deviation 측정 보호가 이유).
- D2 §15는 현행과 일치(검색 결과가 evidence로 유입) — 수정 불요. 단
  "post_search 사고 산출물은 로그 전용이며 어떤 프롬프트에도 재유입되지
  않는다"를 명문화하면 오해가 준다.

### 3.3 번호 인용 계약 (현재 어떤 md에도 없음 — EXPERIMENT_DESIGN.md에 추가)

`community_thinking`의 claim 인용은 2026-07-30부터 번호 기반이다:

1. 노출된 글의 제목·본문 문장에 `[인용 N]` 번호를 부여해 registry를 만든다
   (문장 분리: `.!?…` + 줄바꿈).
2. 모델은 `supporting_quote_ref`(정수 N)만 출력한다. 자유 인용문은 출력하지 않는다.
3. `materialize_claim_quotes`가 N을 원문 문장으로 치환해 `supporting_quote`로
   저장한다 — **구조적으로 축자 일치가 보장**된다(v8 실측 294/294 일치).
4. validator는 `community-thinking-validator-v4`.
5. `source_exposure_ids`에 해당 문장의 출처 노출 ID가 없으면 자동 보충한다.

### 3.4 `RUNBOOK_AND_PREFLIGHT.md`

- **헤더 판정(:3-10) 전면 갱신.** "live canary 수행하지 않았다"는 낡음. 현행:
  - reasoning-off live canary 완료 (Qwen3.5 Flash, alibaba 고정, 전 호출 reasoning_tokens=0)
  - 유료 2일 검증 v8 완주 + `99_validate` segment 통과
  - 무과금 45일 E2E(D2 포함) 완주
  - 판정: **45일 본실험 GO-ready (유료 승인 대기)**
- **게이트 절에 추가**: 오프라인 E2E는 `--max-agents 7` 이상 (§2 참조).
- §9 재봉인 절차는 현행과 일치. 최근 재봉인 이력(`ee13711`→`188b5cf`→
  `82902d9`→`2bffc2c`)을 예시로 갱신하면 좋음.

### 3.5 `AGENTS.md`

- 작업 원칙에 게이트 순서와 `--max-agents 7` 규칙 추가 (§2와 동일 문구).
- :176-178 뱃지 서술은 현행과 일치 — 수정 불요.

### 3.6 `analysis/ANALYSIS_FIELD_GUIDE.md` — 분석 지표 추가

오늘 확정된 관찰 지표를 추가할 것:

- **일별 게시율**: v8에서 70/70명 × 2일 = 140/140 게시(100%). 45일에서 100%
  지속 시 "게시 여부 판단" 설계 요소의 변별력 부재 → 논문 한계 항목.
  원인 후보: hold 비활성으로 매 턴 체결 존재, view_change 상시 비어있지 않음,
  LLM의 null 행동 과소 선택. (프롬프트 편향은 아님 — 균형 확인 완료)
- **인용 다양성**: 6차원 합산 인용 조합 수 (v8: t1 85종 → t3 100종/100명).
  dim_1 단독은 depth당 2~3종으로 좁음(방향 관련 기사가 이벤트당 3~4개인 구조 탓).
- **support 비율**: v8 STB 69.4% / LTB 65.9%. 시장 방향과 무관함을 확인
  (t4 -6.9% 폭락일에 support 75.0%). 뉴스 감성 라벨은 의도적으로 없음.
- **D2 검색 활용**: `agent_turns.jsonl`의 `depth2_flow.step3_search.result_count`,
  STB evidence의 `kind=depth2_recent_search` 인용 수. 45일 라이브에서 첫 실측이 됨.
- **커뮤니티 반응 분포**: v8 like 52.6 / unlike 25.7 / none 21.7 (%).
- **외부 검증**: 삼성전자 실제 개인 순매수/순매도 방향 기록과 시뮬레이션 집계
  방향 비교 예정(사용자 보유). burn-in 3일이 t1 강제 전원매수를 주분석에서 제외.
  hold 비활성이라 빈도 비교는 불가, 방향 비교만 가능.
- 기존 한계 메모 유지: `llm_validation_errors.jsonl`에 agent_id/event_id 필드
  없음 (45일 전 추가 여부 미결).

### 3.7 `README.md`

- :69 뱃지 서술 현행 일치 — 수정 불요. 상태 요약 문구가 있다면 RUNBOOK 헤더와
  같은 기준으로 갱신.

---

## 4. 미결 사항 (다음 세션 인수인계)

1. **45일 본실험 시작** — 사용자 승인 대기. ON/OFF 2 arm, 예상 $40~55.
   시작 명령은 RUNBOOK §실행 절 + `scripts/08_run_six_conditions.py` 경로 참조.
2. `llm_validation_errors.jsonl`에 `agent_id`/`event_id` 추가 여부 — 미결.
   추가하면 코드 변경 → 재봉인 1회 필요.
3. `config.COMMISSION_RATE` — 기록용 외 미사용. 사용자: "지워도 됨, 나중에".
   지우면 run signature가 바뀌므로 다른 변경과 묶어서.
4. 정책 값 다중 하드코딩(카테고리 5/3/2, lookback 7일, MAX_SINGLE_TRADE_CASH_RATIO
   등) — 사용자 결정: **정리하지 않고 그대로 둔다** (오버엔지니어링 방지).
   값 변경 시 감사 보고서의 해당 지점 목록을 먼저 확인할 것.
5. 45일 중 결정론적 실패 발생 시 대응 절차: 원인 수정 → 커밋 → 재봉인 →
   재실행(체크포인트에서 이어짐, 기지불 API 비용은 journal 재생으로 보존됨).
   `kill -9` 수준의 중단도 동일 인자 resume으로 복구됨을 실증했다.

---

## 5. 오늘 발견·수정된 결함 요약 (재발 방지용)

| 결함 | 증상 | 수정 |
| --- | --- | --- |
| LTB dim_6 KeyError (`belief.py:306`) | 모델이 dim_6 구조만 틀리면 재시도 없이 에이전트 즉사, resume 불가 | `.get()` 방어로 정상 거부화 (`34f2ff9`) |
| `make_decision.txt` 키 계약 공백 | 추가 key 출력 시 프롬프트 위반 없이 거부, 재시도 힌트도 무언급 | "필수 key만" 문구 추가 (`34f2ff9`) |
| D2 검색 사망 | 구 전체 일치 채점으로 검색 60/60회 0건, D2≈D1로 붕괴. 에러 없는 조용한 오작동 | 토큰 채점 (`6ecd2c9`), 오프라인 45일로 유입·인용 검증 |
| E2E 커버리지 공백 | `--max-agents 4`에 D2 에이전트 부재 → 검색 코드 미실행 | 게이트를 7명 이상으로 (§2) |
